spring:
  application:
    name: in-the-know-yyc-api
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: on
    hibernate:
      ddl-auto: update
  mvc:
    format:
      date: yyyy-MM-dd
      time: HH:mm:ss
      date-time: yyyy-MM-dd HH:mm:ss
server:
  error:
    include-message: always
    include-binding-errors: always